<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.tms.common.module.business.carcost.dao.CommonCarCostOilCardReceiveDao">

    <select id="selectByWaybillIdAndFuelType"
            resultType="net.lab1024.tms.common.module.business.oilcard.domain.OilCardEntity">
        SELECT
            oil.*
        FROM
            t_car_cost_oil_card_receive receive
                LEFT JOIN t_oil_card oil ON oil.oil_card_id = receive.oil_card_id
        where oil.fuel_type = #{fuelType} and receive.waybill_id = #{waybillId}
        and receive.audit_status != #{excludeAuditStatus}
        GROUP BY
            oil.oil_card_id
    </select>
</mapper>