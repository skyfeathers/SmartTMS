<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.tms.common.module.business.msg.MsgCommonDao">

    <!-- 更新已读状态 -->
    <update id="updateReadFlag">
        UPDATE t_msg
        SET read_flag = #{readFlag},
            read_time = now()
        WHERE msg_id = #{msgId}
          AND receiver_type = #{receiverType}
          AND receiver_id = #{receiverId}
          AND read_flag != #{readFlag}
    </update>

    <update id="updateReadFlagByMsgType">
        UPDATE t_msg
        SET read_flag = #{readFlag},
            read_time = now()
        WHERE msg_type = #{msgType}
          AND receiver_type = #{receiverType}
          AND receiver_id = #{receiverId}
          AND read_flag != #{readFlag}
    </update>

    <!-- 分页查询消息 -->
    <select id="query" resultType="net.lab1024.tms.common.module.business.msg.domain.MsgVO">
        SELECT * FROM t_msg
        <where>
            <if test="query.enterpriseId != null">
                AND enterprise_id = #{query.enterpriseId}
            </if>
            <if test="query.msgType != null">
                AND msg_type = #{query.msgType}
            </if>
            <if test="query.msgSubType != null">
                AND msg_sub_type = #{query.msgSubType}
            </if>
            <if test="query.receiverType != null">
                AND receiver_type = #{query.receiverType}
            </if>
            <if test="query.receiverId != null">
                AND receiver_id = #{query.receiverId}
            </if>
            <if test="query.searchWord != null and query.searchWord !=''">
                AND ( INSTR(title,#{query.searchWord})
                OR INSTR(content,#{query.searchWord})
                )
            </if>
            <if test="query.dataId != null and query.dataId !='' ">
                AND data_id = #{query.dataId}
            </if>
            <if test="query.readFlag != null">
                AND read_flag = #{query.readFlag}
            </if>
            <if test="query.startDate != null">
                AND DATE_FORMAT(create_time, '%Y-%m-%d') &gt;= DATE_FORMAT(#{query.startDate}, '%Y-%m-%d')
            </if>
            <if test="query.endDate != null">
                AND DATE_FORMAT(create_time, '%Y-%m-%d') &lt;= DATE_FORMAT(#{query.endDate}, '%Y-%m-%d')
            </if>
        </where>
        <if test="query.sortItemList == null or query.sortItemList.size == 0">
            ORDER BY msg_id DESC
        </if>
    </select>

    <select id="queryCount" resultType="java.lang.Integer">
        SELECT count(1) FROM t_msg
        <where>
            <if test="query.enterpriseId != null">
                AND enterprise_id = #{query.enterpriseId}
            </if>
            <if test="query.msgType != null">
                AND msg_type = #{query.msgType}
            </if>
            <if test="query.msgSubType != null">
                AND msg_sub_type = #{query.msgSubType}
            </if>
            <if test="query.receiverType != null">
                AND receiver_type = #{query.receiverType}
            </if>
            <if test="query.receiverId != null">
                AND receiver_id = #{query.receiverId}
            </if>
            <if test="query.dataId != null and query.dataId !='' ">
                AND data_id = #{query.dataId}
            </if>
            <if test="query.readFlag != null">
                AND read_flag = #{query.readFlag}
            </if>
        </where>
    </select>


    <!-- 更新全部为已读 -->
    <update id="updateAllReadFlag">
        UPDATE t_msg
        SET read_flag = #{readFlag},
            read_time = now()
        WHERE receiver_type = #{receiverType}
          AND receiver_id = #{receiverId}
          AND read_flag != #{readFlag}
    </update>
</mapper>