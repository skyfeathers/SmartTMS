<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.tms.common.module.business.pic.dao.CommonPicDao">

    <select id="selectByType"
            resultType="net.lab1024.tms.common.module.business.pic.domain.entity.PicEntity">
        select *
        from t_pic
        where type = #{type}
          and enterprise_id = #{enterpriseId}
          and enable_flag = #{enableFlag}
          AND start_time &lt;=now() AND end_time &gt; now()
        order by seq desc ,create_time desc
    </select>



</mapper>