<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.tms.common.module.support.heartbeat.HeartBeatRecordDao">


    <update id="updateHeartBeatTimeById">
        update t_heart_beat_record
        set heart_beat_time = #{heartBeatTime}
        <where>
            heart_beat_record_id = #{id}
        </where>
    </update>

    <select id="query" resultType="net.lab1024.tms.common.module.support.heartbeat.domain.HeartBeatRecordEntity">
      select * from t_heart_beat_record where project_path = #{projectPath} and server_ip = #{serverIp} and process_no =#{processNo}
    </select>

    <select id="pageQuery" resultType="net.lab1024.tms.common.module.support.heartbeat.domain.HeartBeatRecordVO">
        SELECT
        *
        FROM
        t_heart_beat_record
        order by heart_beat_time desc
    </select>

</mapper>
